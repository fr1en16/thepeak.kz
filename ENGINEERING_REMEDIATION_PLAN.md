# План устранения технического долга

Дата начала: 2026-07-02

## Цель

Свести статический анализ к нулю предупреждений, убрать дублирование страниц кейсов, сократить лишние клиентские зависимости и добавить автоматические проверки, не изменив публичные URL, внешний вид и поведение сайта.

## Ограничения

- Перед изменением Next.js-кода сверяться с локальной документацией `node_modules/next/dist/docs/` для установленной версии Next.js 16.2.9.
- Не изменять пользовательские незакоммиченные файлы без необходимости. На старте обнаружены `src/app/web/` и `src/components/ui/portfolio-gallery.tsx`.
- Для русского текста соблюдать правила неразрывных пробелов; динамический текст пропускать через `formatTypography`.
- Галерея кейса должна сохранять сетку 5/4/3/2 и открывать видео fullscreen по клику.

## Исходное состояние

- `npm run lint`: 0 ошибок, 79 предупреждений.
- 16 почти одинаковых страниц кейсов: около 7 600 строк.
- Один `@ts-nocheck`, четыре явных `any`, одна некорректная зависимость `useEffect`.
- Одновременно установлены GSAP, Motion, Framer Motion, Three.js и React Three Fiber.
- Автоматические тесты и обязательные CI-проверки отсутствуют.

## Этап 1. Защитная тестовая база

- [x] Добавить `typecheck` и строгий lint-скрипт с `--max-warnings=0`.
- [x] Настроить Vitest для чистых функций и синхронных компонентов.
- [x] Добавить тесты `formatTypography` и данных кейсов.
- [x] Настроить Playwright smoke-тесты основных маршрутов и кейсов.
- [x] Проверить fullscreen-видео и responsive-сетку галереи.
- [x] Добавить CI: lint, typecheck, unit tests, production build и smoke tests.

Критерий готовности: тестовые команды воспроизводимо запускаются локально; критические сценарии имеют автоматическую проверку.

## Этап 2. ESLint и типы

- [x] Удалить неиспользуемые импорты, переменные и параметры.
- [x] Заменить явные `any` конкретными React/Three.js-типами.
- [x] Удалить `@ts-nocheck` из `liquid-text.tsx` и исправить типы.
- [x] Исправить синхронизацию `PhoneInput` и зависимости `useEffect`.
- [ ] Заменить подходящие `<img>` на `next/image`; технически необходимые исключения документировать локально.
- [x] Вернуть правила TypeScript в режим `error`.

Критерий готовности: `npm run lint -- --max-warnings=0` и `npm run typecheck` проходят.

## Этап 3. Единый шаблон кейсов

- [ ] Инвентаризировать различия старых страниц и данные в `targetCases`.
- [ ] Определить экспортируемый тип `CaseData`, включая hero media, метрики, описание и ссылку.
- [ ] Перенести статические кейсы на `src/app/cases/[slug]/page.tsx`.
- [ ] Сохранить metadata, JSON-LD, URL, hero, галерею и контактную форму.
- [ ] Удалить дублирующиеся `page.tsx` и лишние индивидуальные `layout.tsx` после проверки.
- [ ] Уменьшить клиентскую границу: статический контент оставить серверным, интерактивные части — клиентскими.

Критерий готовности: один динамический шаблон обслуживает все типовые кейсы; маршруты и визуальное поведение сохранены.

## Этап 4. Зависимости и bundle

- [x] Подтвердить отсутствие импортов `gsap` и `@gsap/react`, затем удалить их.
- [x] Унифицировать `motion/react` и `framer-motion` на одном API.
- [ ] Оставить Three.js и R3F только там, где каждый уровень абстракции оправдан.
- [ ] Вынести повторяющуюся инфраструктуру Three.js-графиков.
- [ ] Сравнить production bundle до и после.

Критерий готовности: отсутствуют неиспользуемые или дублирующие зависимости; production build проходит.

## Этап 5. Финальная проверка

- [ ] `npm run lint -- --max-warnings=0`.
- [ ] `npm run typecheck`.
- [ ] Unit/component tests.
- [ ] Playwright smoke tests на desktop и mobile.
- [ ] `npm run build`.
- [ ] Обновить этот документ фактическими результатами и оставшимися рисками.

## Журнал выполнения

- 2026-07-02: зафиксировано исходное состояние; прочитаны локальные руководства Next.js по App Router, Server/Client Components, изображениям, Vitest и Playwright.
- 2026-07-02: ESLint очищен с 79 до 0 предупреждений; typecheck, 4 unit-теста, 6 E2E-тестов и production build проходят. Удалены GSAP и дублирующий пакет Motion.
